package aula4.q1;

public class ObjetoGeometrico {
    // Método para inicializar o objeto (vazio por padrão)
    public void inicializar() {}

    // Método para mostrar os dados do objeto (vazio por padrão)
    public void mostrarDados() {}

    // Método para calcular e retornar a área (vazio por padrão)
    public double calcularArea() {
        return 0.0;
    }

    // Método para calcular e retornar o perímetro (vazio por padrão)
    public double calcularPerimetro() {
        return 0.0;
    }
}
